# Results

Store raw benchmark output in `raw/`, normalized summaries in `processed/`, and
human-readable reports in `reports/`.

Do not overwrite raw results. Add a new timestamped result file for each run.

