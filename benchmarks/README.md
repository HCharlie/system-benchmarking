# Benchmarks

Benchmarks are grouped by the subsystem they primarily measure. Some workloads
will exercise multiple subsystems, but each benchmark should still have one
primary home so results stay easy to compare.

